empezamos en Ubuntu 
sudo apt update && sudo apt upgrade -y
sudo apt install -y git docker.io docker-compose python3 python3-venv python3-pip
v2
sudo apt install -y docker-compose-plugin
paquete de pipx
sudo apt update
sudo apt install -y pipx python3-venv
añadir al path
pipx ensurepath

doodba
pipx install copier
pipx install invoke
pipx install pre-commit

ya después
invoke git-aggregate   # Trae submódulos y dependencias
invoke img-build       # Construye las imágenes Docker
invoke start           # Arranca los contenedores Odoo + Postgres

comando para borrar carpeta
rm -rf ~/odoo_projects/mi_proyecto_odoo

solo crear un proyecto
docker-compose.yml
services:
  web:
    image: odoo:17.0  # Usamos la versión 17 (puedes cambiar a 16 o 18)
    depends_on:
      - db
    ports:
      - "8069:8069"
    volumes:
      - odoo-web-data:/var/lib/odoo
      - ./config:/etc/odoo            # Mapeamos tu config local al contenedor
      - ./addons:/mnt/extra-addons    # Mapeamos tus módulos locales al contenedor
    environment:
      - HOST=db
      - USER=odoo
      - PASSWORD=odoo
    restart: unless-stopped

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - odoo-db-data:/var/lib/postgresql/data/pgdata
    restart: unless-stopped

volumes:
  odoo-web-data:
  odoo-db-data:

odoo.conf
[options]
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
admin_passwd = admin
data_dir = /var/lib/odoo

docker-compose up -d
http://localhost:8069

importante manifest, carpeta security, views
{
    'name': 'Visiotech Test Module',
    'version': '1.0',
    'category': 'Tools',
    'summary': 'Módulo de prueba simple para Doodba/Odoo.',
    'depends': ['base'],
    'installable': True,
    'application': True,
    'license': 'LGPL-3',
    'data': [
        'security/ir.model.access.csv',
        'views/test_model_views.xml',
    ],
}



para subir a git antes crear en git y luego
git remote set-url origin https://github.com/alvaro-fullstack/doodba-copier-template.git
git push -u origin main

